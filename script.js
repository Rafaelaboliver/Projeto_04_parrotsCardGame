parrotGame();

function parrotGame(){
    const numberCards = Number(prompt ('Insira o número de cartas'));
}

//function parrotGame(){
     //for( let i=0; i<14; i++){
       // let card = document.querySelector('.card' + i);
        //card.style.left = (i % 7 === 0)? 0 + 'px' : i % 7 * 117 + 0 + 'px';
        //card.style.top = i < 7 ? 0 + 'px' : 180 + 'px';
     //}


//}
